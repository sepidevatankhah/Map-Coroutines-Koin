<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/map_graph"
    app:startDestination="@id/mapFragment">
    <fragment
        android:id="@+id/mapFragment"
        android:label="MainFragment"
        android:name="ir.nwise.app.ui.map.MapFragment">
        <action
            android:id="@+id/openCarDetail"
            app:destination="@id/carDetailFragment2" />
    </fragment>
    <fragment
        android:id="@+id/carDetailFragment2"
        android:label="CarDetailFragment"
        android:name="ir.nwise.app.ui.detail.CarDetailFragment" />
</navigation>